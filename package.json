{
	"name": "coyote",
	"version": "0.1.0",
	"description": "html templates",
	"type": "module",
	"main": "dist/mod.js",
	"scripts": {
		"prepare": "npm run build",
		"build": "npm run build:dev && npm run build:tests",
		"build:dev": "npx tsc --project src/tsconfig.json",
		"build:tests": "npx tsc --project tests/tsconfig.json",
		"format": "npx prettier --write ./",
		"test": "npx jackrabbit --file ./tests/mod.test.js"
	},
	"devDependencies": {
		"jackrabbit": "github:w-lfpup/jackrabbit-js",
		"prettier": "^3.5",
		"typescript": "^5.7"
	},
	"author": "wolfpup",
	"license": "BSD-3-Clause"
}
